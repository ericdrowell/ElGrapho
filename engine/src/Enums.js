const Enums = {
  renderingMode: {
    PERFORMANCE: 'performance',
    UX: 'ux'
  },
  interactionMode: {
    SELECT: 'select',
    PAN: 'pan',
    BOX_ZOOM: 'box-zoom'
  }
};

module.exports = Enums;